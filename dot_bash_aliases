# aliases
alias gp='git pull'
alias gc='git checkout'
alias gcb='git checkout -b'
alias lg=lazygit
alias activate='source .venv/bin/activate'
# switch project with fuzzy finder on dir name
alias sp='cd $(FZF_DEFAULT_COMMAND="fdfind --type directory --exact-depth 1 . ~/src" fzf)'
